# Phase 2 baseline stack — pinned to the gitignored ml/.venv (Python 3.9, arm64 macOS).
# xgboost needs the OpenMP runtime present (macOS: `brew install libomp`).
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
xgboost==2.1.4
shap==0.46.0            # explainability (used from Phase 4)
matplotlib==3.9.2
pyarrow==17.0.0         # Parquet I/O
pytest==8.3.2          # SPEC_VERSION guard + tests

# Phase 4 deep-learning stack (installed in ml/.venv).
# NOTE: torch and xgboost each ship their own OpenMP runtime and segfault if co-loaded in
# one process on macOS — run_dl.py uses torch only (KMP_DUPLICATE_LIB_OK=TRUE guard).
torch==2.2.2
onnx==1.16.2           # ONNX export (+ SPEC_VERSION / input-contract metadata)
onnxruntime==1.18.1    # PyTorch↔ONNX runtime-parity check
